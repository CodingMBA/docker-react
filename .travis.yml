language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t codingmba/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true codingmba/docker-react npm run test
